@inject NavigationManager Navigation

@code {
    protected override void OnAfterRender(bool firstRender)
    {
        if (firstRender)
        {
            // This runs safely AFTER the component is rendered
            Navigation.NavigateTo("/login", forceLoad: true);
        }
    }
}